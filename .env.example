# Docker environment variables

# Web interface port inside the container.
# Match this with the right side of your Docker ports mapping.
PORT=8080

# Time zone
TZ=UTC

# JWT Authentication Secret.
# Leave empty; docker-entrypoint.sh will generate and persist one in /data/.env.
JWT_SECRET=
